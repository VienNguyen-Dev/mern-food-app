import { Button } from "./ui/button";

export default function MainNav() {
  return (
    <Button variant={"ghost"} className="text-lg font-bold hover:bg-white hover:text-orange-500">
      Log In
    </Button>
  );
}
